@model IEnumerable<Hotel_Management.Models.Carpark>

@{
    ViewData["Title"] = "Index";
}

<h2>Carparks</h2>

<p>
    <a class="btn btn-default" asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarparkLocation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CarparkName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CarparkNumber)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CarparkLocation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CarparkName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CarparkNumber)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
           
            <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
        </td>
    </tr>
}
</table>
<div>
    <input type="button" value="Print" onclick="window.print()" class="btn btn-default" />
</div>
